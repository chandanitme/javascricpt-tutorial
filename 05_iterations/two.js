 //while

  let count=1
 while(count<=10)
 {
    console.log( `value of count is ${count}`)
    count++
 }

 //while with array

 let aaraytest=[ "chandan","atharva","juhi"]
 let index=0
 while(index<=aaraytest.length)
    {
       console.log( `Nme of person is ${aaraytest[index]}`)
       index++
    }

    //do while
     let counttes=10

     do
     {
        console.log( `value of counttst is ${counttes}`)
        counttes++
     }while(counttes <10)